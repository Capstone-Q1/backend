할거

1. requirements.txt에 alembic 추가
2. pip install -r requirements.txt
3. alembic init alembic
4. alembic.ini 설정
5. alembic/env.py에서 SQLAlchemy Base 연결
6. migration 파일 생성
7. DB에 migration 적용

models 수정 → revision 생성 → upgrade 적용

# migration 생성 (models 변경 감지)
alembic revision --autogenerate -m "변경 내용"

# DB 적용
alembic upgrade head


1. postgraSQL은 유저, 비번 필요하지 않나??? env에 셋팅해줘야할텐데
2. clear말고 alembic 적용해서 모델 관리하기

----------------------------------------------------------------------------------------------------

고려사항
1. globalExceptionHandler와 customExceptionHandler를 만들어서 예외처리 및 관리 해야함
2. alembic으로 erd 업데이트 관리 -> ㅅㅂ.. 이거 뭐임... 첨보는데
    -> Alembic = ERD 변경으로 인해 발생한 DB 스키마 변경을 관리하는 도구 ⭕
3.